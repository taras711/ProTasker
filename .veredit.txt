# p - path, f - function name, c - code, ** - comment

-- (p) manager.js
  -> (f) mainData()
    -> (c) [
        (line 215) -> { ...note, contextValue: note.type === 'checklist' ? "checklist" : "filepath", filepath: element.context.file, icon: getIcon(note.type) }
        (line 181) -> { ...note, contextValue: note.type === 'checklist' ? 'checklist' : "dirpath", dirpath: element.context.directory, icon: getIcon(note.type) }
    ]
    ** Определение нового типа **
  
-- (p) package.json
  -> (f) "view/item/context"
    -> (c) [
       (line 295) -> "when": "viewItem == noteItem || viewItem == line || viewItem == filepath || viewItem == dirpath",
       (line 300) -> "when": "viewItem == noteItem || viewItem == checklist || viewItem == line || viewItem == filepath || viewItem == dirpath",
       (line 311) -> "when": "viewItem == filepath",
    ]
    ** Перехват ошибки если файл по указаному пути не найден **
